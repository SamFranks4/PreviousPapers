name: Publish Notion website to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Publish Loconotion Site
        uses: leoncvlt/loconotion@e1c2d3f4g5h6i7j8k9l0m1n2o3p4q5r6s7t8u9v0
