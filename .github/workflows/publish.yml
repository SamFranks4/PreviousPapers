name: Publish Notion website to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Publish Loconotion Site
      uses: leoncvlt/loconotion@<commit-hash>
